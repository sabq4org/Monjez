{
  "version": 2,
  "builds": [
    {
      "src": "monjez_frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "monjez_frontend/dist/index.html" }
  ]
}
